# Stark SDK GUI Requirements

# Stark SDK Installation:
# 1. To install from internal OSS (recommended for pre-release): run `bash install_whl.sh`
# 2. To install from public PyPI: uncomment the line below (if commented out)
# bc-stark-sdk>=2.0.1

# GUI Framework
PySide6>=6.5.0

# Charts (high-performance real-time plotting)
pyqtgraph>=0.13.0
PyOpenGL>=3.1.5  # Required for pyqtgraph 3D visualization
numpy>=1.20,<2.0

# Serial port listing
pyserial>=3.5

# Optional: For better async support
qasync>=0.24.0
